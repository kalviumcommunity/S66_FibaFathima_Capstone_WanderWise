// Placeholder userService for user stats

export async function getUserStats(userId) {
  // TODO: Replace with real API call
  return {
    totalTrips: 0,
    totalSpent: 0,
    upcomingTrips: 0,
    favoriteDestinations: 0
  };
} 