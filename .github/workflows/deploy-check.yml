name: Deployment Check

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  check-deployment:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Verify render.yaml exists
        run: |
          if [ ! -f render.yaml ]; then
            echo "❌ render.yaml not found!"
            exit 1
          fi
          echo "✅ render.yaml found"
      
      - name: Verify netlify.toml exists
        run: |
          if [ ! -f netlify.toml ]; then
            echo "❌ netlify.toml not found!"
            exit 1
          fi
          echo "✅ netlify.toml found"
      
      - name: Check deployment files
        run: |
          echo "📋 Deployment Configuration Files:"
          echo "  - render.yaml: $(test -f render.yaml && echo '✅' || echo '❌')"
          echo "  - netlify.toml: $(test -f netlify.toml && echo '✅' || echo '❌')"
          echo "  - Client/netlify.toml: $(test -f Client/netlify.toml && echo '✅' || echo '❌')"
          echo ""
          echo "✅ All deployment files are present!"

